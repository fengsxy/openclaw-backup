# 2026-03-28 — Saturday

## Autonomous Research: Agent Evaluation

### Production Insights from OpenClaw GitHub

Spent heartbeat researching active OpenClaw ecosystem issues and PRs. Found strong parallels between **system reliability patterns** and **agent evaluation design**:

**Phased Security Model** (PR 56257 - plugin capabilities):
- Declaration → Audit → Enforcement
- Insight: Eval frameworks should adopt phased rollout to reduce risk
- Action: Consider adding "agent self-declaration" phase before strict enforcement

**Heartbeat Starvation Fix** (PR 51657):
- Problem: Continuous queue contention starves heartbeat
- Solution: Track `firstSkippedMs`, force execution after 5min
- Insight: Eval systems must guarantee critical checks run even under load
- Metric: Introduce starvation opportunity cost

**Graceful Shutdown Timeouts** (PR 56258):
- Hung subsystem (Telegram) consumed entire 25s shutdown budget
- Fix: Per-subsystem 5s timeouts, total 25s budget
- Insight: Per-test timeouts + global timeout; log which subsystem hung
- Application: Add hung detection to prevent single test blocking all

**Feature Flag by Default** (TinyFish PR 53114):
- Powerful capability (hosted browser automation) default-off
- Requires explicit opt-in + API key
- Defense-in-depth: URL validation, target blocking, SSE parsing
- Insight: Eval should test "disabled capability" fallbacks

### Framework Evolution

Updated `research/agent-evaluation.md` with Section 8 "生产系统启示录", translating these production lessons into evaluation design principles. The five-dimensional radar (correctness, efficiency, robustness, process quality, safety) now has concrete implementation patterns from real-world systems.

Key refinement: **Phased Evaluation** mirrors plugin capabilities:
1. Declaration: agent states expected behavior boundaries
2. Audit: compare actual vs declared
3. Enforcement: impose hard limits

This reduces false positives and allows incremental tightening.

### Open Question for Yu

What's your take on the plugin capability model? Do you think agent eval frameworks should also adopt a "declaration → audit → enforcement" phased approach?

## System Status

- yunyi quota: expired (0 remaining) — already removed from config
- Primary model: fucheers-claude/claude-opus-4-6 healthy
- Daily log: written (today)
- Memory gap: 2026-03-27 missing — will review during maintenance

## TODOs

- [ ] Update MEMORY.md with today's insights (pending Yu's feedback on phased eval question)
- [ ] Continue research: design concrete experimental tasks