# 2026-02-18

- YouTube/Whisper pipeline: user provided an OpenAI API key for Whisper usage; key was written to `~/.openclaw/secrets/env.sh` (chmod 600) and used successfully to transcribe 张小珺访谈 (DeepMind 谭杰, 121期).
- Fixed `tools/transcribe.py` formatting bug due to OpenAI SDK returning Pydantic objects; later refactored `tools/transcribe.py` to OpenAI-only and added `--cookies` support defaulting to `~/.openclaw/secrets/youtube_cookies.txt`.
- GitHub repo hygiene incident: mistakenly pushed many workspace/private files into `fengsxy/paper_reading`; cleaned by `git rm --cached` + expanded `.gitignore`, and pushed cleanup commits; moved transcript into `transcripts/xiaojun/` to avoid mixing with other content.
- Discovered transcript completeness issue: initial `/tmp/121_tanjie.mp3` and resulting transcript ended around ~20 minutes (~1218s) while YouTube duration is ~2:06:16; regenerated full audio from the downloaded `.webm` to `/tmp/121_tanjie_full.mp3` (~7576s) and started chunking (15 min segments) for re-transcription.
- Cron jobs review: scheduler has multiple jobs (减肥 check-in, daily paper+HN, Karpathy RSS, podcast monitor). Noted one job previously used Groq and hardcoded secrets (token/key) in payload; needs cleanup to OpenAI-only + remove embedded secrets.
- Python/venv pitfall: the repo's `.venv` initially lacked `pip` ("No module named pip"); fix is `python3 -m ensurepip --upgrade` inside the venv. Also note some tools (e.g. `yt-dlp`) live in `.venv/bin` and may not be on PATH unless `source .venv/bin/activate`.

- GitHub Pages is a project page with baseurl `/paper_reading`; timeline entrypoint is `https://fengsxy.github.io/paper_reading/transcripts/xiaojun/`. Updated timeline links to include `{{ site.baseurl }}` and adjusted permalinks to avoid 404s.
- OpenAI key persistence: saved `OPENAI_API_KEY` to `~/.openclaw/secrets/openai_api_key` (chmod 600) and sourced it from both `~/.bashrc` (must be placed before the interactive-shell early-return guard) and `~/.profile` (for login/cron/`bash -lc` shells) to avoid missing env in non-interactive runs.
- ffmpeg failure root cause: MP3 muxer non-monotonic DTS during transcode/segment. Mitigation: switched batch transcription chunking from MP3 to M4A(AAC) and chunk-by-time with `-ss/-t` re-encode to avoid timestamp issues.
- Delivery plan: finish uploading/transcribing the already-downloaded 张小珺 EP123-132 (10 eps) with transcript + analysis per episode, then attempt to pull ~100 episodes for the series, then start on Dwarkesh Patel channel.
- GitHub Pages stability decision: stick to EP121 pattern for 张小珺 episodes (flat `transcripts/xiaojun/<slug>.md` + `<slug>.analysis.md` with full Jekyll front matter and `permalink: /transcripts/xiaojun/<slug>/`); avoid directory `index.md` + `include_relative` variants since they previously caused 404/deploy breakage.
- OpenClaw model config request pending: user asked to add a new custom provider (OpenAI-compatible base URL) with a specific model id (`claude-opus-4-5-20251101`). Config/schema were inspected; no config patch was applied yet. Do not store API keys in repo; keep them under `~/.openclaw/secrets/`.

- Fucheers provider debugging: `/v1/models` returned only `claude-opus-4-5-20251101-thinking`; using `claude-opus-4-5-20251101` caused HTTP 403 (key lacks access). Working endpoint is OpenAI-style `POST /v1/chat/completions` (responses endpoint not implemented). Provider config was adjusted accordingly and default model switched back to `yunyi-codex/gpt-5.2` with Fucheers kept as backup.
- Whisper batch speedups: `scripts/xiaojun_batch_transcribe.py` updated to support concurrent chunk transcription (`--max-workers`, default 4) and longer default chunks (30min, `--segment-seconds 1800`), plus option to disable chunking (`--segment-seconds 0`) for single-shot transcription.
- Robust key loading: `scripts/xiaojun_batch_transcribe.py` falls back to reading `~/.openclaw/secrets/openai_api_key` if `OPENAI_API_KEY` env var is missing; additionally, `.bashrc` must load keys before the interactive guard and `.profile` is used for login/cron shells.
- Disk management: moved already-transcribed podcast audio to `data/audio/xiaojun/_archived_transcribed/<timestamp>/` instead of keeping all `.webm` files in the main audio folder.
- Scholar Inbox automation: installed `scholarinboxcli` into the repo `.venv` after adding pip via ensurepip; generated daily digest files under `scholar_inbox/`.
- README cleanup: Scholar Inbox section changed to a small Date/Highlights table (like Karpathy RSS) and moved detailed listings to `scholar_inbox/index.md`.
- Cron security cleanup: removed hardcoded GitHub token and API keys from cron payload texts; updated daily paper+HN job to activate venv and push via SSH; updated podcast monitor job to avoid embedded secrets.
- Cron validation: manually triggered `Daily Paper & HN Update` and confirmed it generated `scholar_inbox/2026-02-18-diffusion-dllm.md` + `hackernews/2026-02-18.md` and pushed commit `99407ac`.
- Preference update from Yu: for Fun Videos expansion, keep default pipeline on Scheme A (RSS/feed-first, no mandatory paid API). For future exploration search, prefer You.com YDC Search API; do not build new Brave Search-dependent automation.
- Product-thinking notes from Yu: emphasize process externalization (`process.md`/`memory.md`/`sop.md`) because agent internal memory is not guaranteed; prioritize no-friction memory continuity, better auto-compact/context awareness, and duplex/multi-task status reporting (A/B/C task progress visibility) to reduce stale replies and mismatch across queued requests.
- Operational update: switched OpenClaw main model/provider to Yunyi Codex with added `gpt-5.3-codex` option and alias `codex53`; updated provider API mode to `openai-completions` after verifying `/v1/chat/completions` works while `/v1/responses` failed.
- User preference reaffirmed: future exploration search should use You.com YDC API (not Brave). YDC key stored locally at `~/.openclaw/secrets/ydc_api_key` and exported from `~/.bashrc` as `YDC_API_KEY`.
- Added hourly cron `OpenClaw Best Practices Watch` (PST hourly) that uses YDC to check OpenClaw best-practice updates and only applies low-risk actionable workflow changes when needed.
- Podcast indexing milestone: created automated full-index pipeline via `scripts/build_podcast_indexes.py` + `scripts/podcast_channels.json`; generated/pushed indexes under `podcast_index/` for Xiaojun (151), Dwarkesh (185), and 罗永浩十字路口 (23).
- Xiaojun watchdog enhancement: changed hourly transcribe watchdog from hardcoded EP whitelist to dynamic queue-based detection using `scripts/xiaojun_queue.jsonl` slugs; checks transcript existence + analysis existence + `TBD` placeholders.
- Security/config hygiene: installed and configured Claude Code runtime via local secrets (`anthropic_auth_token`, `anthropic_base_url`) loaded from `~/.bashrc`; no keys committed to repo.
- Long-running task management preference confirmed by Yu: move long jobs into screen-managed sessions in main host context.
- Implemented: migrated Xiaojun transcription worker to a fixed screen session `xiaojun_transcribe`, with logs at `.logs/xiaojun_transcribe.log`; stopped duplicate non-screen transcribe process before starting screen session.
- Explicit user expectation: podcast long-run target is ~300 episodes/tasks and must be proactively advanced without waiting for reminders; require recurring progress accountability updates.
- New standing preference from Yu: perform a daily homepage freshness check (verify homepage date rows include today's updates) and proactively fix/push if missing.
- Homepage freshness issue found and fixed: 2026-02-18 Scholar/Karpathy/Hackernews pages existed, but README homepage rows were partially stale. Patched README and pushed commits (`e04f5f7`, `4a0c1ce`), then verified homepage shows all three dated entries with HTTP 200.
- Added persistent automation for homepage freshness: cron `Daily Homepage Freshness Check` now validates same-day Scholar Inbox + Karpathy RSS + Hackernews presence on README and online URLs, and auto-fixes + pushes when missing.
- Whynot expansion: built full channel index (23 videos) and created filtered priority queue `scripts/whynot_queue.jsonl` (top 5 selected by Yu); added `transcripts/whynot/selected_top5.md` as visible shortlist.
- Xiaoyuzhou pipeline discovery progressed: Apple Podcasts lookup for LinkStart (`id=1693180879`) returned stable feed URL `https://feed.xyzfm.space/q9a6lueucj6a` with 104 episodes; task moved to in-progress for indexing/queue/timeline.
- Bilibili pipeline planning added: reference repo `JimLiu/whisper-subtitles`; first validation target fixed to `https://space.bilibili.com/1140304215?spm_id_from=333.337.0.0`.
- Search strategy update accepted by Yu: OpenClaw best-practice watch switched from YDC-dependent querying to Claude Code-first research against official sources, with failure-safe no-risk-change behavior.
- Podcast 300 Progress Drive hourly run hit timeout; follow-up plan is to split the job into smaller steps (progress stats/health check first, then incremental execution) to avoid 15-minute timeouts.
- Xiaojun latest verified status (manual check): queue count 11, missing transcript 0, missing analysis 0, analysis TBD 0; current tracked batch (121 + 123-132) is fully complete for transcript+analysis.
- Yu requested reducing homepage low-signal content; removed the entire `🔬 Research Notes` section from `README.md` and pushed commit `34b227a` to keep homepage focused on high-value daily updates.
- Yu clarified repository policy: remote GitHub should be blog-content-only; code/pipeline files should not appear on remote. Applied by untracking `scripts/` and adding ignore rules; keep local tooling available.
- Yu explicitly requested pause/abandon Xiaojun line for now. Disabled `Xiaojun Transcribe Watchdog`, stopped xiaojun transcribe processes, and updated 300-progress cron intent to prioritize `Dwarkesh -> WhynotTV -> Xiaoyuzhou`.
- Naming/reporting update requested by Yu: stop calling it "300 mainline"; use actual scope counts. Current retained target scope (excluding Xiaojun) totals 312 (Dwarkesh 185 + Whynot 23 + Xiaoyuzhou 104).
- Dwarkesh download feasibility verified: without cookies fails bot check; with existing cookies succeeds and format list is available.
- Executed Dwarkesh pilot download: first 10 episodes downloaded successfully to `data/audio/dwarkesh/` (10/10).
- Whynot site usability issue fixed after Yu reported homepage/timeline discovery and 404 problems. Added Whynot entry on README timeline and normalized permalink/front matter for new Whynot transcript/analysis pages. Pushed commit `9f9e9dc`.
- Communication preference reinforced by Yu: when blocked, ask directly instead of silently stalling; in progress reports proactively answer recurring user questions and include concrete fix actions.
- Post-fix verification for Whynot pages completed at ~21:54 UTC: Whynot index and episode links return HTTP 200 (`/transcripts/whynot/`, `whynot_chen_tianqi_ml_systems`, `whynot_huyuanming_meshy_taichi`, `whynot_yangshuo_optimus_humanoid`, and `004_whynottv_wengjiayi_openai_rl_infra`).
- User reported "clicking interviews in Whynot has no effect" even after fix; likely client-side cache. Recommended hard refresh/incognito and offered UI tweak (more explicit link/button styling) if issue persists.
